name: "Research SKILL Activation Test"
test_target:
  type: skill
  name: research
input:
  prompt: "Use the research skill to investigate TDD best practices"
expected_output:
  contains:
    - "evidence-based"
    - "systematic"
  not_contains:
    - "error"
    - "failed"
timeout: 60
