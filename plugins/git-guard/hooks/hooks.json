{
  "$schema": "../../schemas/hooks-schema.json",
  "description": "Git Guard hooks - prevents bypassing git hooks",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/commit-guard.sh"
          }
        ]
      }
    ]
  }
}
